name: indonesian-emotion-classification

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      preprocessing_dir:
        type: str
        default: "twitter_emotion_preprocessing"
      random_seed:
        type: int
        default: 42

    command: >
      PREPROCESSING_DIR={preprocessing_dir}
      RANDOM_SEED={random_seed}
      python modelling.py
